# Kubernetes Ingress, ConfigMaps & Secrets — Screenshots

## Ingress addon enabled

![minikube addons enable ingress](01-ingress-addon.png)

## ConfigMap

![kubectl apply/get/describe configmap + jsonpath](02-configmap.png)

## Secret

![base64 encode, kubectl apply/get/describe secret + decode](03-secret.png)

## Ingress — path-based routing (frontend `/` + backend `/api/`)

![kubectl apply/get/describe ingress + curl tests](04-ingress-routing.png)
